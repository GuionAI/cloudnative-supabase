{{- if .Values.leaderElection.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "cloudnative-supabase.fullname" . }}-leader-election
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cloudnative-supabase.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "cloudnative-supabase.fullname" . }}-leader-election
subjects:
  - kind: ServiceAccount
    name: {{ include "cloudnative-supabase.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
