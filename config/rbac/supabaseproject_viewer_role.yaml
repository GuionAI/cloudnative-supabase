# This rule is not used by the project cloudnative-supabase itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to supabase.guion.dev resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: cloudnative-supabase
    app.kubernetes.io/managed-by: kustomize
  name: supabaseproject-viewer-role
rules:
- apiGroups:
  - supabase.guion.dev
  resources:
  - supabaseprojects
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - supabase.guion.dev
  resources:
  - supabaseprojects/status
  verbs:
  - get
